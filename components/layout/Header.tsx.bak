import Link from "next/link";
import { SITE_NAME } from "@/lib/nav";
import { ClubLogo } from "@/components/ui/ClubLogo";
import { SiteNav } from "./SiteNav";

export function Header() {
  return (
    <header className="relative z-50">
      <div className="mx-auto max-w-7xl px-3 pt-3 sm:px-4">
        <div className="glass-dark flex h-14 items-center justify-between rounded-2xl px-4 sm:h-16 sm:px-6">
          <Link href="/" className="flex items-center gap-3 text-white">
            <ClubLogo size="sm" decorative preload />
            <span className="leading-tight">
              <span className="block font-display text-base font-semibold tracking-wide sm:text-lg">
                FK Pobjeda
              </span>
              <span className="block text-[11px] uppercase tracking-[0.18em] text-gold">Triješnica</span>
            </span>
            <span className="sr-only">{SITE_NAME}</span>
          </Link>
          <SiteNav />
        </div>
      </div>
    </header>
  );
}
